{
  "name": "customrapid-plugin-registry",
  "version": "1.0.0",
  "private": true,
  "description": "Plugin registry and plugin distribution repo for Rapid Plugin Manager",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "lint": "eslint .",
    "test": "node --test",
    "validate": "node ./scripts/validate-registry.mjs",
    "check": "npm run lint && npm run test && npm run validate"
  },
  "devDependencies": {
    "@eslint/js": "^9.25.1",
    "eslint": "^9.25.1",
    "globals": "^16.0.0"
  },
  "license": "MIT"
}
